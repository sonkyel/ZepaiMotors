# Copia este archivo como ".env.local" y rellena el valor.
# NO subas .env.local al repositorio (ya está ignorado en .gitignore).

# URL del webhook de n8n que recibe los leads del formulario.
# La obtienes en n8n, en el nodo "Webhook" (Production URL).
N8N_WEBHOOK_URL=
